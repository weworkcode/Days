# Days
Declare and initialize a string array like this:  String[] days = {“sunday” , “monday”, "tuesday","wednesday", "thursday", "friday", "saturday" };  Then prompt the user to enter day number(0-6), then print out the day which corresponds to the user input.  Once done please upload your file on canvas.
